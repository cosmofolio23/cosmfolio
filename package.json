{
  "name": "cosmfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "CosmoFolio - AI Architecture Portfolio Generator (Monorepo: Next.js frontend + FastAPI backend)",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf .next/cache",
    "start": "cd frontend && npm run start",
    "dev": "cd frontend && npm run dev"
  }
}
