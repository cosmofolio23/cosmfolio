# Cloudflare Pages Configuration
# Tells Cloudflare to only build the frontend folder

[build]
  # Only build the Next.js frontend
  base = "frontend"
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
